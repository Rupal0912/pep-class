// merge two sorted singly linked list (in place)
#include<iostream>
using namespace std;
class Node{
    int data;
    Node* next;
    Node(int val): data(val), next(nullptr){}

};

Node* mergesortedLisnkedLists(Node)